# 이어서 작업 메모

**저장 시각**: 2026-05-15 (오전 세션)
**작업 디렉토리**: G:\-업무\히즈\.claude\0. 데일리 뉴스레터

## 1. 지금까지 한 일

### 버그 발견
- 사용자가 "오늘 인사말이 뒤에가 잘렸어" 보고
- `output_20260515.txt` 77번 라인 마지막 문장이 마침표·이모지 없이 "이번 주 최후반 흐름도 잘"에서 끊김
- `docs/newsletters/2026-05-15.html`, `docs/v2/newsletters/2026-05-15.html` (GitHub Pages 송출본)도 동일 위치에서 잘림 확인

### 근본 원인
- `formatter.py`의 세 LLM 호출 함수(`_call_claude` / `_call_groq` / `_call_gemini`) 모두 `max_tokens=1000`
- 한국어는 1글자 ≈ 2~3 토큰이라 3문단 인사말 + 셀프 비평 재작성에 1000 토큰이 빠듯
- 5/14 인사말은 1506 byte로 정상 종료, 5/15는 1244 byte인데 잘림 → 셀프 비평 패스에서 입력 토큰 증가 + 출력 한계 도달로 추정

### 수정 1: max_tokens 상향 (다음 발송분부터 적용)
- `formatter.py:394` (Groq) `max_tokens` 1000 → 2000
- `formatter.py:411` (Gemini) `maxOutputTokens` 1000 → 2000
- `formatter.py:430` (Claude) `max_tokens` 1000 → 2000

### 수정 2: 5/15 잘린 인사말 보정 (3개 파일)
- 원본: `금요일 아침 햇살처럼 화사하게 하루 보내시고, 이번 주 최후반 흐름도 잘`
- 수정: `금요일 아침 햇살처럼 화사하게 하루 보내시고, 이번 주 막바지도 잘 마무리하시기 바랍니다. 한 주 수고 많으셨습니다 ☀️`
- 적용 파일:
  - `output_20260515.txt`
  - `docs/v2/newsletters/2026-05-15.html` (메인 v2)
  - `docs/newsletters/2026-05-15.html` (v1)

### Git 작업
- commit `9002951` "fix: 인사말 max_tokens 1000→2000 상향 + 5/15 잘린 인사말 보정"
- `git push origin main` 완료
- 커밋에는 `formatter.py`, `docs/newsletters/2026-05-15.html`, `docs/v2/newsletters/2026-05-15.html` 3개만 포함
- `github_push.py`, `image_gen.py`는 이전 세션 잔여 변경사항 — **건드리지 않음**

### 메모리 업데이트
- `C:\Users\green\.claude\projects\G----------claude-0----------\memory\project_newsletter.md`에 "## 인사말 max_tokens 상향 (2026-05-15 수정)" 항목 추가

## 2. 현재 상태
- GitHub Pages 빌드: ✅ `built` 상태 (커밋 `9002951` 빌드 완료, 00:47:49Z)
- raw GitHub 파일: ✅ 새 버전 반영 확인
- CDN 캐시 우회 요청 (`?v=...`): ✅ 새 버전 응답
- 일반 라이브 URL: ⚠️ 사용자 브라우저 기준 옛 버전 보임 → CDN 캐시 만료 대기 (~10분) 또는 강력 새로고침 필요
- 사용자에게 `Ctrl+Shift+R` 안내 완료

## 3. 다음에 할 일
- [ ] 5/16 발송 시 인사말이 끝까지 생성되는지 확인 (max_tokens=2000 적용 효과 검증)
- [ ] 잘림 문제 재발 시 1차 생성 vs 셀프 비평 패스 중 어디서 잘리는지 진단 print 추가 검토
- [ ] (선택) `github_push.py`, `image_gen.py`의 미커밋 변경사항이 무엇인지 확인 후 처리 — 이번 세션에서는 손대지 않음
- [ ] (선택) 사용자가 CDN 캐시 만료 후 라이브 페이지 정상 확인 보고 받기

## 4. 관련 파일·경로
- `formatter.py` — max_tokens 상향 적용 (3곳)
- `output_20260515.txt` — 5/15 텍스트 결과물 (잘린 인사말 보정)
- `docs/v2/newsletters/2026-05-15.html` — GitHub Pages 메인 (잘린 인사말 보정)
- `docs/newsletters/2026-05-15.html` — v1 (잘린 인사말 보정)
- `C:\Users\green\.claude\projects\G----------claude-0----------\memory\project_newsletter.md` — 수정 이력 메모 추가됨

## 5. 결정사항·맥락
- max_tokens는 출력 토큰 한계라 비용 영향 거의 없음 → 안전 마진을 위해 2000으로 상향
- 잘린 인사말 보정 시 "최후반"이라는 어색한 표현은 "막바지"로 다듬고, weekday_msg("한 주 수고 많으셨습니다!") 톤을 자연스럽게 결합
- `github_push.py`, `image_gen.py`의 이전 세션 변경사항은 **이번 작업 범위 밖**으로 명시적으로 제외 (사용자 의도 미확인)

## 6. 미해결·블로커
- 사용자 브라우저 기준 라이브 페이지 캐시가 언제 갱신되는지 미확인 (CDN Fastly TTL ~10분 추정)
- `github_push.py`, `image_gen.py` 미커밋 변경사항 정체 불명

## 7. 다음 세션 시작 프롬프트 제안
> 5/16 인사말이 정상적으로 끝까지 생성됐는지 `output_20260516.txt` 확인하고, 잘림 문제 재발 여부 보고해줘.
