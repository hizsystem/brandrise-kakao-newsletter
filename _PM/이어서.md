# 이어서 작업 메모

**저장 시각**: 2026-05-22 09:30
**작업 디렉토리**: G:\-업무\히즈\.claude\0. 데일리 뉴스레터

## 1. 지금까지 한 일 (최신순)

### 2026-05-22 (오늘)
- **빌더조쉬 config 구조 단순화** (수/금 분리 → 단일 슬롯 통합)
  - 사용자 결정: "미리 알 수 없는 URL이라 어차피 발송 직전 갱신 필요" → 슬롯 1개로 충분
  - `config.yaml`: `wednesday_newsletters` 제거 + `friday_newsletters.builder_josh` 제거 → top-level `builder_josh` 신설
  - `main.py`: 수요일/금요일 분기 통합 → `if weekday in (2, 4)` 한 블록에서 `config["builder_josh"]` 처리
  - `CLAUDE.md` (프로젝트): 요일별 수집 소스 + config 설명 동기화
  - `config.example.yaml`: 동일 구조로 동기화

- **인사말에 빌더조쉬 첫 등장 자동 안내 로직 추가**
  - 사용자 요청: "오늘 빌더조쉬 처음이니 인사말에 그 내용으로 작성"
  - `formatter.py`에 `_first_appearance_sources()` 헬퍼 신설 — 과거 `output_*.txt`를 스캔해 한 번도 등장한 적 없는 stibee source 자동 감지
  - `_build_greeting_prompt`의 `notes` 배열에 첫 등장 노트 자동 주입 (공지 톤이 아니라 본문 흐름에 자연스럽게 녹임)
  - 미래에 새 stibee 소스가 추가되어도 첫 발송 1회만 자동 트리거됨
  - 오늘 output 재생성 → 인사말에 빌더조쉬 닉 바실레스쿠 이야기 중심으로 자연 통합 완료
  - GitHub Pages도 새 commit으로 푸시 (오늘 두 번째 푸시)

### 2026-05-20 이전 (역사)
- 빌더조쉬(maily.so/josh) 뉴스레터 수집 기능 신규 도입
- `collectors/builder_josh.py` 작성 (og:title/description + `<article>` 본문 → Claude→Groq→Gemini 폴백으로 3줄 요약 자동 생성)
- 기존 `StibeeNewsletter` dataclass 재사용 (formatter / HTML v2 호환)
- 검증 완료: `python -m collectors.builder_josh`, `python main.py --preview`

## 2. 현재 상태

- 코드 변경 모두 워킹 트리에만 있음 (아직 미커밋)
  - `M config.yaml` (여전히 gitignore — 빌더조쉬 슬롯 통합 반영)
  - `M config.example.yaml` (커밋 대상)
  - `M main.py` (커밋 대상 — 수/금 분기 통합)
  - `M formatter.py` (커밋 대상 — 첫 등장 자동 감지 헬퍼)
  - `M CLAUDE.md` (커밋 대상 — 통합 구조 설명)
  - `M output_20260522.txt` (오늘치 재생성)
  - `M docs/...` (HTML 재생성)
  - `M _PM/이어서.md` (이 파일)
- GitHub Pages는 오늘 두 번 푸시됨 (1차: 기존 인사말, 2차: 빌더조쉬 중심 인사말로 덮어쓰기)

## 3. 다음에 할 일

- [ ] **워킹 트리 변경 커밋·푸시** (사용자 승인 후)
  - feat: 빌더조쉬 config 수/금 단일 슬롯 통합
  - feat: 인사말에 첫 등장 stibee 소스 자동 안내 로직
- [ ] **2026-05-27(수) 아침**: `config.yaml`의 `builder_josh.url`을 그날 새 글 URL로 교체 후 `python main.py --now` (또는 스케줄러)
- [ ] **2026-05-29(금) 아침**: `builder_josh.url`을 또 새 글로 덮어쓰고 발송 (까탈로그는 Gmail 자동 추출)
- [ ] (선택) Gemini 모델 ID `gemini-2.0-flash` → `gemini-1.5-flash` 교체 (v1beta에서 404 가능성)

## 4. 관련 파일·경로 (현재 구조 기준)

- `config.yaml` (gitignore)
  - `builder_josh.url` — 수/금 발송 직전 수동 갱신 (단일 슬롯)
  - `tuesday_newsletters` — 풋풋레터·캐릿
  - `friday_newsletters` — 까탈로그만 남음
- `main.py:186~199` — 수/금 빌더조쉬 통합 블록 (`if weekday in (2, 4)`)
- `main.py:201~245` — 금요일 까탈로그 블록 (Gmail 자동 → 수동 폴백)
- `formatter.py:138~169` — `_first_appearance_sources()` 헬퍼 (신규)
- `formatter.py:_build_greeting_prompt` — 첫 등장 노트 주입 로직
- `collectors/builder_josh.py` — maily.so 본문 + LLM 3줄 요약
- `CLAUDE.md` — 요일별 수집 소스 / config 항목

## 5. 결정사항·맥락

- **수/금 빌더조쉬 → 단일 슬롯**: 두 URL을 미리 알 수 없고 어차피 발송 직전 갱신 필요. 슬롯 분리해도 작업량 동일 → config 단순성이 이김.
- **첫 등장 자동 감지**: `output_*.txt` 전체 스캔 비용은 무시 가능 (파일 몇십 개). 영구 로직으로 두면 미래 신규 소스 추가 시 1회 자동 안내. 광고 톤이 아니라 본문 흐름 녹임 지시 포함.
- **금요일 출력 순서**: 빌더조쉬 → 까탈로그 → 롱블랙 (이전 결정 유지, `stibee_items.insert(0, item)` + dict 순회로 보장)
- **LLM 폴백 체인**: Claude → Groq → Gemini (인사말·요약 공통)
- **`config.yaml` gitignore**: API 키 보호 차원, 의도된 동작 유지

## 6. 미해결·블로커

- 없음. 모두 검증 완료.
- (저강도) Gemini 모델 ID 404 가능성 — Claude/Groq 폴백으로 안전망 충분.

## 7. 다음 세션 시작 프롬프트 제안

> "데일리 뉴스레터 빌더조쉬 config가 수/금 단일 슬롯으로 통합됐고, 인사말에 첫 등장 stibee 소스 자동 안내 로직도 들어갔어. 오늘 5/22(금) 발송분에 빌더조쉬가 정상 합류했고 첫 등장 안내도 인사말에 자연스럽게 녹았어. 다음 발송은 5/27(수)이고 그날 아침 `config.yaml`의 `builder_josh.url`만 새 글로 갱신하면 됨."
