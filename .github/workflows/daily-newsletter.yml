name: Daily Newsletter
on:
  workflow_dispatch:
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: secret test
        env:
          A: ${{ secrets.ANTHROPIC_API_KEY }}
          G: ${{ secrets.GEMINI_API_KEY }}
          N: ${{ secrets.NVIDIA_API_KEY }}
        run: echo "A=${#A} G=${#G} N=${#N}"
